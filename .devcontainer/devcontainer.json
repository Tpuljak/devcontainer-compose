// For format details, see https://aka.ms/devcontainer.json.
{
    "name": "Docker Compose Test Container",
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/workspace",
    "initializeCommand": ".devcontainer/initialize.sh"
}